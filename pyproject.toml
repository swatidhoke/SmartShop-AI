[project]
name = "smartshop-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Swatidhoke", email = "Swatidhoke@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.14",
    "langchain-huggingface>=1.2.2",
    "langchain-openai>=1.4.3",
    "langgraph>=1.2.10",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.2",
    "streamlit>=1.61.1",
]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
